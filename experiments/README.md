# Active experiments

Experiment scripts in this directory are thin, reproducible entry points for
the rebuilt REQ-ML pipeline.

Historical runners and analyses are preserved under:

```text
archive/legacy_v1/experiments
```

Scientific implementations belong in `src/+reqml`, not inside experiment
scripts.
